# A2 设计基线交叉复核记录

- 日期：2026-09-22；输入 main：`561c14b61533638be5ea2bbd9163428c5cb1d14c`。
- 拟议文档 A 保持：`79f73faedcd9cde4164b0d1625782dae27db6c2f`。
- 规则 R 保持：`10d2a5c827964989f41ca6e8eeac3d44de6d0f04`。
- scope：`LH-A2-EXEC-MCP-v1`；首批 E1–E3；Gate **OPEN**。
- 承接 [核对接口与预检阶段复核](RECOVERY_CONTRACT_REVIEW.md)。

## 结论

三路独立只读审查及主审交叉复核未确认新的必改设计缺口。
本轮保留需求、架构、实施方案的原字节和既有 A，只追加复核事实；没有扩大功能、接口或验收门槛。
当前方案可提交 Owner 对准确基线和 E1–E3 范围作出开工决定。
这是设计审查结论，不是新 job/MCP/Plugin 已实现或运行验收通过。

## 反向推演与现有约束

| 路径 | 已有阻断规则 | 验收映射 |
| --- | --- | --- |
| 核对受理/完成后回执丢失，重送误启新核对 | 同 reconcile_id 只重取原轮；新 id 才申请新观察 | V03 |
| 业务已取消后启动独立核对，旧业务取消重试误停核对 | target 明确区分业务和准确核对轮次，不推断最新一次 | V04 |
| 查询返回 NOT_FOUND，但旧请求仍在传输 | 不换 id；取消未受理不报取消完成；I/O 不明单独处理 | V03/V04 |
| 旧 helper 超时仍存活，反复重试耗尽容量或并发写入 | UNKNOWN 保留容量/资源屏障，同父核对互斥，不反复增建 helper | V04/V05/V08 |
| 预检与业务意图循环依赖，或预检后事实变化 | 辅助/业务意图分开；受保护映射许可预检，业务启动复核代次、取消、租约与事实绑定；慢 I/O 在围栏外 | V01/V04 |
| writer 仍运行、DB seal 失败或新核对追加 seal，却报告完整交付 | 静止才完整封存；DURABILITY_UNKNOWN 不开放读取；旧 seal 不改写，全件及成员核验拒绝混拼 | V07/V08 |
| 伪来源、授权撤销、发现版本变化或失败 prepare 进入后续作业 | 固定来源与当前主体/资源授权；STALE 不自动改绑；只有成功且完整 SEALED 返回可执行 prepared 引用 | V01/V09/V10 |
| 当前客户端只获得摘要/截图或 base64 文本就算交付 | E2 宿主回调与 writer；E4 实际至少 16 MiB ZIP 取回与续传；能力缺失保持 BLOCKED | V08/V10 |
| E4 与 GX10 切换循环等待，或模拟冒充真实 NAS | E4 可用独立测试后端；E3 真实隔离 cgroup 是候选门槛；E5 部署与 E6 NAS 分开验收 | V11/V12 |

执行专项另直接核对固定 Ledger NAS 脚本 blob
`3dc8642cb3034db0e5451b5bf6dc6f4137f27f73` 与运行手册 blob
`53b90d2f503e6772ee244ee60003f00627639177`：四参数、无 resume、独立验证后精确删除本轮本地合成副本、
独立恢复与对账均与既有方案一致。没有运行真实 NAS 操作。

审查区分了 E1 内的实现定型与设计缺口：状态转换的具体阶段码、客户端 ID 保存位置、游标编码、
以及按作业类型关联证据可在既有约束内实现。host.inspect 的固定输入是 profile，观察节点/目标环境；
prepare/测试关联生成后的准确候选。不能要求前置 inspect 伪造尚未生成的 wheel，也不因此新增输入或门槛。

## 本轮实际检查

在编辑前，对输入 main 的既有源码运行隔离检查，Python `3.12.14`，生成物均在仓库外。

| 检查 | 实际结果 |
| --- | --- |
| Python 编译 | `python -X pycache_prefix=<isolated-cache> -m compileall -q tools tests setup.py`；52 个既有 Python 文件；退出 0；UTC 开始 `2026-09-22T13:47:32.517294+00:00` |
| shell 语法 | `bash -n tools/local_hand/bootstrap_linux.sh`；退出 0；UTC 开始 `2026-09-22T13:47:32.838960+00:00` |
| 输出 | 两命令各 stdout/stderr 共四份空日志；SHA-256 均为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`；检查后工作树 clean |
| 准确基线 | 三份权威文档逐字节等于 A；8 个相对链接有效；12 项需求、9 个架构节、12 项验收和 7 个 MCP 工具映射完整 |
| 发布范围 | 仅本复核记录及根 AGENTS 的记录链接；运行源码、三层文档和 S1/S2 文档保持原字节；差异空白、链接和公开披露模式检查通过 |

没有重跑全量测试、构建安装 wheel、创建新生产/测试源码、依赖、Plugin 或运行配置。
没有投递实机任务、切换服务或修改 NAS。历史测试计数不计入本轮结果。

## 后续

A 不变，本记录不 supersede 它，也不是 Owner closure 或 CLOSED 登记。
下一步仍按根 AGENTS 与既定 R，由 Owner 对准确 A 和 E1–E3 作出明确决定，
单独登记 CLOSED 后再实施。E4 实际连接、E5 GX10/S2 和 E6 真实 NAS 继续各自满足后续门槛。
