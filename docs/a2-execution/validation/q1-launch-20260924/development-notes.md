# 本轮开发与独立复核记录

本记录区分已保存的原始日志、作者报告和独立源码复核，不重建缺失的历史日志。

1. 启动器首次 CLI 专项中，模拟 `sys.platform = 'win32'` 的子进程未预加载 `shutil`，
   `runpy` 后续导入标准库触发不存在的 `_winapi`，出现一项测试失败。作者仅调整测试，
   在模拟平台前导入 `shutil`，随后报告专项 20 passed、0 skipped。首次失败的原始输出
   存于当时会话工具结果，未保存为磁盘日志；本次未补跑或伪造该历史日志。
2. 独立复核发现若把管理采集期限限制在原 query deadline 内，会裁掉既有控制器超时后
   最多 3 秒的收尾诊断。修改为外部事先固定、最长 120 秒的管理采集窗口；原 ticket、
   query issued/deadline 不变。真实管道用例覆盖原查询超时后到达的 UNKNOWN 诊断。
3. 固定 `.py` 清单还须排除加载优先级更高的入口。加入 Python 识别的扩展模块后缀拒绝。
   独立复核用 `FileFinder.find_spec` 复现同名 package 目录优先于 `.py`；最终补充所有
   固定模块同名路径拒绝，并在实际临时路径覆盖 `experiment/__init__.py` 与
   `admission/__init__.py`。未执行这些影子包的代码。
4. 根复核的首轮整组 235 项已经通过，原日志保存为
   `unittest-before-package-shadow-fix.log`；该轮发生在第 3 项同名 package 修复之前，
   没有当时完整源码摘要，不能作为最终候选的验证替代。修复后再次执行整组，最终
   `unittest.log` 为 235 passed、0 skipped，4.494 秒，退出码 0。
5. 真实 exec 测试显式设定有限 `RLIMIT_CPU=(9,9)`，并检查 exec 前后 PID、双管道身份
   与 CPU rlimit 连续。临时路径的权限/祖先检查明确使用替身；真实写入、短写、fsync、
   create-only 和 exec 保留，不能把这些用例记为真实宿主准入。

独立审查者最终只读确认以上修复和交接文档一致，在本轮限定源码/合同/测试设计范围内
无剩余阻断；未重复执行作者专项测试，也未执行 systemd/quota。根执行最终整组和边界检查。
审查没有要求另增停止适配器或扩大本轮范围。

源码本地提交后，推送公开 GitHub main 被自动审批拒绝，理由是未识别到明确发布授权。
核对历史后未找到针对当前 Q1 候选的明确公开 main 发布原话，故未通过其他工具绕过。
本轮源码和开发证据保留为独立本地提交；该候选 GitHub CI 尚未运行。

以上为初始证据提交时的发布状态。用户随后明确允许发布两个检查点及核对 CI；
已通过 GitHub 连接发布完全相同的文件树。提交编号映射见 `publication-map.json`，
当前发布/CI 状态见 `publication-status.json` 和本轮验证报告。保留首次拦截历史。
