# A2 执行链第三轮设计复核

- 日期：2026-09-22；输入 main：`674f104213a9b26b44457254e66da9f9f97722f6`。
- 原拟议 A：`bc54edc503a6a88aaca1d98276e403b9d97c6743`；R 保持
  `10d2a5c827964989f41ca6e8eeac3d44de6d0f04`。
- 范围：`LH-A2-EXEC-MCP-v1` 三层设计及既有代码的隔离检查；Gate 仍 OPEN。
- 本记录承接 [上一轮复核](DESIGN_RECHECK.md)，不改写其历史输入或结果。

## 确认问题与修订

本轮采用三路独立只读复核：接口/授权、Linux/固定 Ledger 工具适配、状态/证据；
主审对照准确文档和原始依据确认下列三项。它们是尚未实现方案中的缺口，不能描述为已修复运行中的 MCP。

| ID | 具体失败路径 | 已修订的约束 | 需求／架构／验收 |
| --- | --- | --- | --- |
| DC-01 | submit 必须填写准入引用及六项 expected；接口只承诺脱敏部署/状态，prepare 成功也没有明确返回 prepared_ref，客户端无法继续构造请求 | capabilities 返回按主体过滤、分页且绑定版本的逻辑引用和完整 expected；私有准入目标先校验；成功且 SEALED 的 prepare 经 status/manifest 返回同一稳定引用与来源绑定；STALE 不自动改绑重投 | R01/R09；A03/A07/A08；V01/V10 |
| DC-02 | 启动前鉴权成功后，本地 grant 被撤销，但此前已交进程管理器的延迟请求仍可能启动 | 本地撤销/策略代次与启动共用已有围栏，失效旧预留并核对所有延迟请求；回执区分准入已关闭、排队启动封阻、运行中停止；不明保留资源屏障 | R04/R08；A03/A05/A08；V09 |
| DC-03 | NAS 预检或 reconcile 同步进入控制循环/数据库写事务/启动围栏；hard NFS 等待使 status/cancel 一并停住，主程序尚未 spawn 的检查也不受作业监督 | 所有可能阻塞的存储检查、哈希和核对交给受监督 helper/runner，先登记归属和预算；控制面短时回执，本地账本健康时查询/取消仍可受理；超时不证明退出，不释放资源或不断增建 helper | R04；A05/A06/A07；V04 |

DC-03 的平台依据是 [Linux nfs(5)](https://man7.org/linux/man-pages/man5/nfs.5.html)：
hard 请求可以持续重试；改为 soft 存在数据完整性风险，不能用这种变更代替执行隔离。
这是据平台行为推演的设计失败路径，本轮没有对真实 NAS 制造故障。

## 反向检查与保留边界

| 检查方向 | 结果与限制 |
| --- | --- |
| 意图后崩溃、NAS 已发布但回执丢失、证据文件已发布而 DB seal 失败 | 既有 UNKNOWN/DURABILITY_UNKNOWN、原 id 核对、不重跑与资源屏障规则仍一致；本轮未确认新缺口 |
| 取消时 cgroup 暂空但有延迟启动、reconcile 追加证据版本、ZIP 成员与全件摘要 | 已有规则覆盖；DC-02 复用其围栏，没有另造执行器或恢复协议 |
| 固定 Ledger 安装态工具与 prepared 环境 | 已核对固定源码 `6bd6acfbe5c35d581891eb87275e1173e17848fc`；原位只读解释器/安装 payload 与独立可写源码副本兼容，不移动 venv |
| 新旧协议与 S2 切换 | 新 job 不进入旧 v1 mailbox，不通过旧 validation 安装 MCP；双账本/冻结/回退约束未变，本轮不修订 S2 三层文档 |
| 权限和交付 | 引用发现不授予执行权限；撤销登记不证明进程停止；进程超时不证明 I/O 停止；证据生成不等于当前客户端已收到文件 |

接口和本地受监督 I/O 的负例先在 E1–E3 合成环境验证；实际客户端与连接绑定留到 E4，
E3 的隔离 Linux 真实 cgroup 集成门槛不变，GX10 监督器与部署绑定在 E5 复验，
NAS 阻塞等真实隔离故障留到 E6。不能把设计反例或单元模拟计为实机 PASS。
修订后的三路回审另澄清：原业务取消不阻止获准的固定只读核对，核对使用独立执行身份，
仍受同一资源屏障、启动围栏和当前授权约束；没有借 helper 重新启动原业务程序的例外。

## 本轮实际验证

既有源码输入准确为本页首列 main；Python `3.12.14`。编译和 shell 检查于编辑文档前完成，
均在隔离工作树运行，缓存位于仓库外，结束后工作树仍 clean。

| 检查 | 命令／结果 |
| --- | --- |
| 既有 Python 编译 | `python -X pycache_prefix=<isolated-cache> -m compileall -q tools tests setup.py`；52 个既有 Python 文件；退出 0；UTC 开始 `2026-09-22T12:49:00.348967+00:00` |
| Linux shell 语法 | `bash -n tools/local_hand/bootstrap_linux.sh`；退出 0；UTC 开始 `2026-09-22T12:49:00.614381+00:00` |
| 命令输出 | 两条命令分别保存 stdout/stderr；四份空日志 SHA-256 均为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| 文档检查 | 修订后的相对链接、R/A 引用、需求/架构/验收映射、差异空白与公共披露模式检查通过；全部非 Markdown 文件、S1 三文档及 S2 文档原字节保持不变 |

本轮未重新运行全量测试、wheel 构建或安装，也没有新增生产/测试代码、依赖、Plugin 包或运行配置。
编译通过只证明既有源码的语法；不将历史测试计数或上轮结果算作本轮新功能验证。
公开记录仅保留脱敏检查结果，不发布主机路径、身份、凭据或原始实机证据。

## 准确基线与下一步

本次仅 supersede `LH-A2-EXEC-MCP-v1` 的原拟议 A；新的文档提交完整 SHA 由随后独立登记提交写入
根 AGENTS，仍为 OPEN，登记不是 CLOSED C。S2 拟议 A 保持 `bc54edc503a6a88aaca1d98276e403b9d97c6743`。
S1 已有 closure 和 Ledger 本体 A2 已有 closure 不变。

下一步仍是 Owner 对既有准确 A、R 和 E1–E3 作出明确决定，再单独记录 CLOSED，之后才实施。
这是根 AGENTS 和固定 Gate 对新 scope 的要求；本轮“再检查、直接修复”支持本次文档修订，
不冒充对修订后尚未形成准确 A 的新实现授权。E4/E5/E6 不包含在首批实施范围内。
