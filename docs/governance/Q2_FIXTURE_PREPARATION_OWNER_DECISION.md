# Q2 隔离 fixture 准备 Owner 开工决定

- Decision authority / speaker：Owner，本会话用户。
- Record event ID：`LH-Q2-FIXTURE-PREP-CLOSURE-20260926-01`，仓库事件标识，不冒充平台消息 ID。
- Decision submission time：`2026-09-26T15:24:05+08:00`，本会话提供的消息时间。
- Source：Owner 对下述已提交基线及范围的直接批准；可核实原文保留于本文件。
- Stable retained reference：本文件在独立 CLOSED 登记 C 中的副本；准确 C 由 Git history 定位。
- R：`10d2a5c827964989f41ca6e8eeac3d44de6d0f04`；直接规则已读取，SHA-256 与根 AGENTS 一致。
- A：`2ea59b8d1b262632bae5636938107ef2f002a59b`。
- Scope：`LH-Q2-FIXTURE-PREP-v1`。

## 已存在的准确基线及批准副本

此前已提交的 [基线记录](https://github.com/kongbu0621/infra-local-hand/blob/b94973ca32111957b906090cd7f37103ab02bf39/docs/governance/Q2_FIXTURE_PREPARATION_BASELINE.md)
明确固定上述 R、A、三份权威文档摘要和新增对象/资源上界。助手请求批准该基线、关闭本 scope 并整批继续；
随后状态答复再次说明该 scope 涵盖新账户、七个配额目录及一次“预检 → 业务 → 证据”监督运行。

Owner 本次完整原文：

> **批准上述基线，关闭 `LH-Q2-FIXTURE-PREP-v1` Gate，整批继续。** 此范围内不再逐项询问。

“上述基线”绑定已提交的准确 R/A 和 scope；不合成 Owner 未说过的文本。
这次决定授权集中实现、验证和交付，以及准确既有隔离 Q1 guest 内的新增对象准备和一次有限原监督接续。

## 授权边界

仅新增一名普通账户/主组及其实例级 user manager 委派，五类独立执行父级，两组三根及一个独立 store
共七个新 project 域，以及准确新安装、普通空账本、管理 journal、输出与声明对象。
逐域不超过 64 MiB/4096 inode，七域合计不超过 448 MiB/28672 inode；安装暂存不超过 256 MiB，
普通 state/准备日志不超过 32 MiB，管理 journal、捕获/声明分别不超过 64 MiB。
一次固定 `host.inspect` 原监督运行保留既有不超过 120 秒的合同和独立退出/双流证据。

保留全部 Q1 分配及失败记录。禁止格式化、mount/fstab 改动、包/内核升级、通用 user manager 模板修改、
旧对象重用、自动重放/回收、GX10/现役 S1/真实 NAS 改动。准备或模型测试不等于 Q2/Q3、生产或 E4–E6 接纳。
现有工具、容量、控制器不满足时准确报告 BLOCKED；不从子级成功字段推导外部所有者退出。

本 C 仅保存 Owner 批准及 CLOSED 状态，不改变 A 的三份文档，不加入源码、测试、依赖或运行配置。
实现 D 必须以 C 为祖先。范围内继续执行，不重复逐项询问；实质范围变化仍按根变更规则处理。
